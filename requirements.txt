# Core dependencies
requests>=2.31.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0

# Data processing
pandas>=2.0.0  # Optional: 데이터 분석용

# Type hints and validation (optional but recommended)
pydantic>=2.0.0  # 데이터 검증용
typing-extensions>=4.7.0  # Python 3.8 이하 호환성

# Testing (development only)
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0

# Code quality (development only)
black>=23.0.0
flake8>=6.0.0
mypy>=1.4.0
isort>=5.12.0

# Monitoring and logging (optional)
rich>=13.0.0  # 향상된 콘솔 출력
tqdm>=4.65.0  # 진행률 표시